---
'@backstage/plugin-app': minor
---

Updated the app runtime to use the current navigation, analytics, icon, and dialog APIs. Custom navigation components now receive `navItems` without the deprecated flat `items` collection.
